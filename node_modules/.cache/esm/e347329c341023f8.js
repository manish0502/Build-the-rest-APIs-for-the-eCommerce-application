let express,APP_PORT,routes;_341‍.w("express",[["default",["express"],function(v){express=v}]]);_341‍.w("./config",[["APP_PORT",["APP_PORT"],function(v){APP_PORT=v}]]);_341‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);

const app = express();




app.use(express.urlencoded({ extended: false }));
app.use(express.json());
app.use('/api',routes)

app.listen(APP_PORT ,() =>{
    console.log(`App is listening on port ${APP_PORT}`);
})